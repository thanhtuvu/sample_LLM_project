# create and switch to a new branch
git checkout -b v1-rag-elasticsearch

# stage all changes
git add .

# commit
git commit -m "v1: RAG pipeline with Elasticsearch and Claude Haiku"

# push to remote
git push -u origin v1-rag-elasticsearch

# switch to main
git checkout main

# merge the branch
git merge v1-rag-elasticsearch

# push main to remote
git push origin main

#list all index names:
curl http://localhost:9200/_cat/indices?v